name: Build and Release Extension Charts via PR creation

on:
  workflow_dispatch:
  release:
    types: [released]

defaults:
  run:
    shell: bash
    working-directory: ./

jobs:
  create-pr-build-extension-charts:
    uses: rancher/dashboard/.github/workflows/create-pr-build-extension-charts.yml@9eb70a732e9be146722e1dbab431338366c2afc6 # creators-pkg-v3.0.10
    permissions:
      actions: write
      contents: write
      deployments: write
      pages: write
      pull-requests: write
    with:
      target_branch: main
      tagged_release: ${{ github.ref_name }}
